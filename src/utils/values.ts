const normalBorder: string = '1px solid black';
const errorBorder: string = '2px solid red';

const normalBackgroundColor: string = 'white';
const errorBackgroundColor: string = 'pink';

export { normalBorder, errorBorder, normalBackgroundColor, errorBackgroundColor };